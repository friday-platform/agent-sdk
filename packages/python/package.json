{
  "name": "@friday/python",
  "version": "0.1.1",
  "private": true,
  "scripts": {
    "test": "uv run pytest",
    "lint": "uv run ruff check .",
    "fmt": "uv run ruff format .",
    "check": "uv run ruff check . && uv run ruff format --check ."
  }
}
